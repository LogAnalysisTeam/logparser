from setuptools import find_packages, setup

setup(
    name='logparser',
    packages=find_packages(),
    version='0.1.0',
    description='loglizer: A toolkit for automated log parsing',
    author='LOGPAI, Log Analysis Team, AIC, FEE CTU in Prague',
    license='MIT',
    install_requires=[],
)
